<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <!--
            The MainActivity class declaration, LaunchMode, ConfigurationChanges,
            and the ActionSend intent-filter are all handled by the [Activity] and
            [IntentFilter] attributes in MainActivity.cs. Declaring them here too
            would produce duplicate entries in the merged manifest.
        -->
    </application>
</manifest>
